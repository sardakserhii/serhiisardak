---
import { CONTACTS } from "../lib/contacts";
import { Icon } from 'astro-icon/components'
---

<section
  class="min-h-screen bg-[#16181D] flex flex-col items-center justify-around gap-5 overflow-hidden bg-[url(/src/assets/bg/bg-lines-symmetric.svg)] bg-center bg-no-repeat bg-size-[120%]"
>
  <div class="flex flex-col items-center text-center gap-2">
    <p class="text-[#BB72E9] text-[20px]">Contacten</p>
    <p class="font-bold text-2xl text-gray-100">Wollen Sie mir Schreiben?</p>
    <p class="text-[16px] text-gray-200">
      Runter finden Sie meine Contant-Linken
    </p>
  </div>
  <!-- Contact list -->

  <div class="w-full max-w-md gap-1 flex flex-col">
    {
      CONTACTS.map((contact) => (
        <div class="flex items-center space-x-3 my-2 bg-gray-800 p-3 rounded-lg hover:bg-gray-700 transition-colors ">
            <Icon name={contact.icon} class="w-6 h-6 text-gray-400" />
           <a
            href={contact.value}
            class="text-gray-200 hover:text-white "
            target="_blank"
            rel="noopener noreferrer"
          >
            <span class="font-medium">{contact.label}:</span>
            <span>{contact.value}</span>
          </a>
           <Icon name="lucide:arrow-up-right" class="w-4 h-4" />
        </div>
      ))
    }
  </div>

  <p>
    Ich bin immer offen für neue Möglichkeiten und freue mich darauf, von Ihnen
    zu hören!
  </p>
</section>
